<h1>Editing Concentration #<%= @concentration.id %></h1>

<%= form_tag(concentration_url(@concentration), method: 'patch') do %>
  <div>
    <%= label_tag :name %><br />
    <%= text_field_tag :name, @concentration.name %>
  </div>

  <div>
    <%= submit_tag "Update Concentration" %>
  </div>
<% end %>
